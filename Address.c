#include <stdio.h>

int main()
{

    int no = 22;

    printf("Value of no is : %d\n", no);

    printf("Sizeof of no is : %d\n", sizeof(no));

    printf("Address of no is : %d\n", &no);

    return 0;
}